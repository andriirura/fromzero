using System;

namespace Homework
{
    class Program
    {
        static void Main(string[] args)
        {
            //1.Середнє арефметичне двох чисел.

            //double firstNumber, secondNumber;

            //Console.Write("Enter the one number: ");
            //firstNumber = double.Parse(Console.ReadLine());

            //Console.Write("Enter the two number: ");
            //secondNumber = double.Parse(Console.ReadLine());

            //double result = (firstNumber + secondNumber) / 2;

            //Console.Write("Result = " + result);

            int index = 25;//z
            index += 2;//key
            int abcLenght = 26;
            if (index > 25)
            {
                index -= abcLenght;
            }
        }
    }
}
